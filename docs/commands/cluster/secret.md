# Команды Cluster Secret 🔐

Команды для управления ресурсами `ClusterSecret`. В отличие от обычных `Secret`, которые привязаны к неймспейсу, эти секреты являются глобальными для всего кластера.

## 📝 Создание (Create)

Создает новый `ClusterSecret` из переданных пар ключ-значение.

```bash
dodo cluster secret create <name> [key=value...]
```

### Пример
```bash
dodo cluster secret create global-config api_url=[https://api.example.com](https://api.example.com) token=secret-value -c d-yandex
```

### Параметры
- `--context`, `-c`: Контекст Kubernetes.
- `--yes`, `-y`: Пропустить подтверждение создания.

---

## 📋 Список (List)

Выводит таблицу со всеми существующими `ClusterSecret` в кластере.

```bash
dodo cluster secret list
```

### Параметры
- `--context`, `-c`: Контекст Kubernetes.

---

## 🔍 Просмотр (Get)

Показывает информацию о секрете: имя, возраст и количество ключей. Сами значения скрыты в целях безопасности.

```bash
dodo cluster secret get <name>
```

### Параметры
- `--context`, `-c`: Контекст Kubernetes.

---

## 🗑 Удаление (Delete)

Удаляет указанный секрет из кластера.

```bash
dodo cluster secret delete <name>
```

### Параметры
- `--context`, `-c`: Контекст Kubernetes.