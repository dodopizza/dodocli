# Команда: `list` 📋
Выводит список ресурсов `DodoAppService` в кластере. Отображает статус, время последнего запуска пайплайна и таймауты.
## Использование
```bash
dodo app list [flags]
```
## Флаги
| Флаг | Сокращение | Тип | Описание |
| :--- | :--- | :--- | :--- |
| `--namespace` | `-n` | `string` | Неймспейс для поиска. Обязателен, если не указан флаг `--all`. |
| `--all` | `-a` | `bool` | Показать ресурсы во всех неймспейсах. |
| `--context` | `-c` | `string` | Контекст Kubernetes. |
## Примеры
### Список приложений в конкретном неймспейсе
```bash
dodo app list -n sre -c d-yandex
```
### Список всех приложений во всех неймспейсах
```bash
dodo app list -a
```