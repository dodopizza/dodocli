# Команды Request 🆘

Инструмент для быстрого создания обращений к дежурным инженерам (SRE/Platform) через сервис **Duty Helper**.

## Использование

```bash
dodo request
```

Команда запустит интерактивный режим, где предложит:
1. Выбрать затронутый сервис.
2. Указать критичность (Severity).
3. Ввести заголовок и описание проблемы.

В результате вы получите ссылку на созданную карточку (тикет), которую возьмет в работу дежурный.

### Требования
Для работы команды необходимо, чтобы был настроен API ключ и email. Обычно это происходит автоматически при выполнении `dodo init` (через авторизацию в Azure).